---

---

<section class="brand-intro">
  <h2 class="brand-intro__title">Amplify Creative LAB</h2>

  <p class="brand-intro__tagline">Where Stories Amplify into Impact</p>

  <p class="brand-intro__lead">
    <span
      >In the digital world, you need content that truly resonates with your
      customers. It’s about transforming ordinary moments into</span>
    <span>extraordinary experiences. </span>
  </p>

  <p class="brand-intro__body">
    <span
      >Everyone claims to be a photographer and content creator, but you still
      need us—a full-service creative studio. Our photography goes beyond
      capturing moments; it visualises stories and ignites emotions across the
      digital landscape.Let the experts handle the professional work, freeing
      you to focus on what
    </span>
    <span>
      truly matters—running your business and spending quality time with loved
      ones. With Amplify, you amplify your impact, not your stress. That’s what
      Amplify Creative Lab is all about.
    </span>
  </p>
</section>

<style>
  .brand-intro {
    background-color: var(--e-global-color-8e31e7a, #24360f);
    color: var(--e-global-color-primary, #fef8eb);
    height: 40vh;
    /* In Elementor, padding is on the inner container, 
     so here we keep it 0 and let the inner handle spacing */
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px;
    gap: 30px;
  }

  /* Title */
  .brand-intro__title {
    /* From Elementor heading */
    font-family: "Abril Fatface", sans-serif;
    font-weight: 400;
    font-size: 60px;
    /* desktop size */
    letter-spacing: 1px;
    color: var(--e-global-color-primary, #fef8eb);
    margin: 0;

    /* Elementor sets line-height: 0 on this widget, but that's
     risky outside that environment (text can clip).
     Using a tight line-height instead to visually match. */
    line-height: 1;
  }

  /* Tagline under the title */
  .brand-intro__tagline {
    /* From Elementor 2a12bf2 */
    font-family: "Darker Grotesque", sans-serif;
    font-size: 24px;
    font-weight: 400;
    line-height: 1;
    color: var(--e-global-color-primary, #fef8eb);

    /* Match Elementor's tighter paragraph spacing */
  }

  .brand-intro__text:last-of-type {
    margin-bottom: 0;
  }

  /* Button */

  .brand-intro__lead {
   /* tweak to taste */
  margin: 0 auto;       /* center the block */
  text-align: center;

  font-family: "Darker Grotesque", sans-serif;
  font-size: 35px;
  font-weight: 200;
  font-style: italic;
  line-height: 35px;
  color: var(--e-global-color-primary, #fef8eb);
}

.brand-intro__lead span {
  display: block;
}

.brand-intro__lead span + span {
  margin-top: 0.5rem;   /* spacing between lines */
}


  .brand-intro__body {
    max-width: 100%;

    white-space: nowrap;
margin: 0 auto;     /* horizontally center the <p> */
  text-align: center; /* center the text inside */
    font-family: "Darker Grotesque", sans-serif;
    font-size: 18px;
    font-weight: 300;
    line-height: 23px;
    color: var(--e-global-color-primary, #fef8eb);

    /* Elementor sets p:last-child margin-bottom:0 
     and uses widget gap for vertical spacing,
     so we keep paragraphs marginless and rely on flex gap */
 
  }

  /* Mirror: .elementor-widget-text-editor p:last-child { margin-bottom: 0; } */
  .brand-intro__body:last-child,
  .brand-intro__lead:last-child {
    margin-bottom: 0;
  }
  .brand-intro__body span {
    display: block;
  }

  /* Mobile tweak – you can adjust to taste */
  @media (max-width: 768px) {
    .brand-intro__inner {
      padding: 60px 20px 20px;
    }
  }

  /* Optional: scale down on smaller screens */
  @media (max-width: 1024px) {
    .brand-intro__title {
      font-size: 46px;
    }
  }

  @media (max-width: 768px) {
    .brand-intro__title {
      font-size: 36px;
    }
  }
</style>
